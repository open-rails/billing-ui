# Canonical OpenRails wire fixtures

Copied verbatim from `testdata/wire/` in github.com/open-rails/openrails at
`dca35f2f3` (PR #494, #809 customer recovery, stacked on the v1 candidate
#452 + #453 + #479) and decoded by `src/core/schemas.test.ts`. Re-copy when
the source moves.
